# Catalog Variables

> These tools were removed. Use `query_records` / `create_record` / `update_record` on table `item_option_new` instead.
>
> See the catalog architecture blueprint for field names and filter examples.
